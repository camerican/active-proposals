<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>Our Class</title>
</head>
<body>
	<h1>Our Class</h1>
	<table>
	<tr>
		<th>First</th>
		<th>Last</th>
		<th>Proposals</th>
	</tr>
	<% @users.each do |user| %>
		<tr>
			<td><%= user.fname %></td>
			<td><%= user.lname %></td>
			<td><% if user.proposals %><ol>
			<% user.proposals.each do |proposal| %>
				<li><%= proposal.name %></li>
			<% end %>
			</ol>
				<% end %></td>
		</tr>
	<% end %>
	</table>
</body>
</html>